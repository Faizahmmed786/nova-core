server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: novacore-backend

  datasource:
    url: jdbc:postgresql://db.eqyegnerrgjfofcvehxz.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: YHqbfqDLIIAqXzYh
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 20

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    time-zone: UTC

    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: ${JWT_SECRET:novacore_secret_key_very_long_and_secure_2024}
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

google:
  client-id: ${GOOGLE_CLIENT_ID:your-google-client-id.apps.googleusercontent.com}

app:
  mining:
    points-per-claim: 40
    claim-interval-hours: 4
    total-daily-slots: 6

  referral:
    register-bonus: 400
    mining-bonus: 100
    per-claim-bonus: 2

  frontend-url: ${FRONTEND_URL:http://localhost:3000}